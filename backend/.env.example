SECRET_KEY=replace-with-openssl-rand-hex-32
DATABASE_URL=sqlite:///./neuron.db

ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: for local SDK demos (sdk/demo_ring_narrative.py). Copy to .env — never commit .env.
# Create a key in the app: Settings → API Keys → Generate.
NEURON_API_KEY=nrn_replace_with_key_from_settings
