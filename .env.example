# Copy to `.env` in this directory for Docker Compose (backend SECRET_KEY).
NEURON_SECRET_KEY=replace-with-openssl-rand-hex-32
# Optional demo controls passed through to the backend container.
# DEMO_MODE_ENABLED=true
# BOOTSTRAP_DEMO_USER=true
